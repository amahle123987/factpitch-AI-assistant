# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env overrides (VITE_API_BASE_URL isn't secret — it's baked into the
# client bundle either way — but keeping local overrides out of git avoids
# committing someone's personal port/host setup)
.env
.env.local

.env
__pycache__/
*.pyc
.venv/
venv/
data/
*.log
.pytest_cache/
.idea/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
